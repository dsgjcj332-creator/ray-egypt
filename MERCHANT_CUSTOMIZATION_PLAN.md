# 📋 خطة تخصيص لوحة تحكم المتاجر

## 🎯 الهدف الرئيسي
إعطاء صاحب كل نشاط (مطعم، محل، عيادة، إلخ) التحكم الكامل في صفحة عرض نشاطه

---

## 📊 المخطط المعماري

```
┌─────────────────────────────────────────────────────────────┐
│                    Ray Egypt Platform                       │
└─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                │             │             │
        ┌───────▼────────┐ ┌──▼──────────┐ ┌──▼──────────┐
        │   Restaurants  │ │   Stores    │ │  Bookings   │
        │   (مطاعم)      │ │ (محلات)     │ │ (حجوزات)    │
        └────────────────┘ └─────────────┘ └─────────────┘
                │                │              │
        ┌───────▼────────┐ ┌──▼──────────┐ ┌──▼──────────┐
        │  Merchant      │ │  Merchant   │ │  Merchant   │
        │  Dashboard     │ │  Dashboard  │ │  Dashboard  │
        │  (تحكم)        │ │  (تحكم)     │ │  (تحكم)     │
        └────────────────┘ └─────────────┘ └─────────────┘
                │                │              │
        ┌───────▼────────┐ ┌──▼──────────┐ ┌──▼──────────┐
        │  Restaurant    │ │  Store      │ │  Booking    │
        │  Display Page  │ │  Display    │ │  Display    │
        │  (عرض)         │ │  Page       │ │  Page       │
        └────────────────┘ └─────────────┘ └─────────────┘
```

---

## 🏗️ البنية الفنية

### 1. قاعدة البيانات (MongoDB)

```javascript
// Merchant Schema
{
  _id: ObjectId,
  name: "اسم النشاط",
  type: "restaurant|store|clinic|salon|nursery", // نوع النشاط
  owner: ObjectId, // صاحب النشاط
  
  // إعدادات التخصيص
  customization: {
    colors: {
      primary: "#FF6B6B",
      secondary: "#4ECDC4",
      accent: "#FFE66D",
      background: "#FFFFFF",
      text: "#333333"
    },
    
    // التحكم في العناصر
    elements: {
      showHero: true,
      showGallery: true,
      showReviews: true,
      showMenu: true, // للمطاعم
      showProducts: true, // للمحلات
      showBookings: true,
      showContact: true,
      showMap: true
    },
    
    // الأزرار والإجراءات
    buttons: {
      primaryText: "اطلب الآن",
      primaryColor: "#FF6B6B",
      secondaryText: "اعرف أكثر",
      secondaryColor: "#4ECDC4",
      showWhatsApp: true,
      showPhone: true,
      showEmail: true
    },
    
    // الخطوط والأنماط
    typography: {
      fontFamily: "Cairo",
      headingSize: "large",
      bodySize: "medium"
    },
    
    // الصور والوسائط
    media: {
      heroImage: "url",
      logo: "url",
      gallery: ["url1", "url2", ...]
    }
  },
  
  // البيانات الأساسية
  description: "وصف النشاط",
  address: "العنوان",
  phone: "رقم الهاتف",
  email: "البريد الإلكتروني",
  
  createdAt: Date,
  updatedAt: Date
}
```

---

## 📁 هيكل المشروع

```
frontend/src/
├── app/
│   ├── merchant/
│   │   ├── [id]/
│   │   │   ├── page.tsx (صفحة عرض النشاط)
│   │   │   ├── layout.tsx
│   │   │   └── components/
│   │   │       ├── MerchantHero.tsx
│   │   │       ├── MerchantGallery.tsx
│   │   │       ├── MerchantMenu.tsx (للمطاعم)
│   │   │       ├── MerchantProducts.tsx (للمحلات)
│   │   │       ├── MerchantBookings.tsx
│   │   │       └── MerchantReviews.tsx
│   │   └── dashboard/
│   │       ├── [id]/
│   │       │   ├── page.tsx (لوحة التحكم)
│   │       │   ├── layout.tsx
│   │       │   └── components/
│   │       │       ├── ColorCustomizer.tsx (تخصيص الألوان)
│   │       │       ├── ElementToggler.tsx (إظهار/إخفاء العناصر)
│   │       │       ├── ButtonCustomizer.tsx (تخصيص الأزرار)
│   │       │       ├── MediaUploader.tsx (رفع الصور)
│   │       │       ├── Preview.tsx (معاينة مباشرة)
│   │       │       └── Settings.tsx (الإعدادات العامة)
│   │       └── restaurants/
│   │           ├── page.tsx
│   │           └── components/
│   │               ├── MenuCustomizer.tsx
│   │               ├── CategoryManager.tsx
│   │               └── MenuPreview.tsx
│   │
│   ├── restaurants/
│   │   ├── page.tsx (قائمة المطاعم)
│   │   ├── [id]/
│   │   │   └── page.tsx (صفحة عرض المطعم)
│   │   └── components/
│   │       ├── RestaurantCard.tsx
│   │       ├── RestaurantHero.tsx
│   │       ├── RestaurantMenu.tsx
│   │       └── RestaurantBooking.tsx
│   │
│   ├── stores/
│   │   ├── page.tsx (قائمة المحلات)
│   │   ├── [id]/
│   │   │   └── page.tsx (صفحة عرض المحل)
│   │   └── components/
│   │       ├── StoreCard.tsx
│   │       ├── StoreHero.tsx
│   │       ├── StoreProducts.tsx
│   │       └── StoreBooking.tsx
│   │
│   └── bookings/
│       ├── page.tsx (قائمة الحجوزات)
│       ├── [id]/
│       │   └── page.tsx (صفحة الحجز)
│       └── components/
│           ├── BookingCard.tsx
│           ├── BookingForm.tsx
│           └── BookingConfirmation.tsx
│
├── components/
│   ├── merchant/
│   │   ├── MerchantCard.tsx (بطاقة النشاط)
│   │   ├── MerchantHero.tsx (الصورة الرئيسية)
│   │   ├── MerchantGallery.tsx (المعرض)
│   │   ├── MerchantReviews.tsx (التقييمات)
│   │   └── MerchantContact.tsx (التواصل)
│   │
│   └── customizer/
│       ├── ColorPicker.tsx (منتقي الألوان)
│       ├── ElementToggle.tsx (تبديل العناصر)
│       ├── ButtonCustomizer.tsx (تخصيص الأزرار)
│       ├── PreviewPanel.tsx (لوحة المعاينة)
│       └── SettingsPanel.tsx (لوحة الإعدادات)
│
├── hooks/
│   ├── useMerchantCustomization.ts (إدارة التخصيص)
│   ├── useMerchantData.ts (جلب بيانات النشاط)
│   └── useCustomizationPreview.ts (معاينة التخصيص)
│
├── services/
│   ├── merchantService.ts (API للنشاطات)
│   ├── customizationService.ts (API للتخصيص)
│   └── uploadService.ts (رفع الصور)
│
└── types/
    ├── merchant.ts
    ├── customization.ts
    └── booking.ts
```

---

## 🔄 تدفق العمل

### للعميل (صاحب النشاط):

```
1. تسجيل الدخول
   ↓
2. الذهاب إلى لوحة التحكم
   ↓
3. تخصيص الصفحة:
   - اختيار الألوان
   - إظهار/إخفاء العناصر
   - تخصيص الأزرار
   - رفع الصور
   - معاينة مباشرة
   ↓
4. حفظ التغييرات
   ↓
5. صفحة العرض تتحدث تلقائياً
```

### للزائر:

```
1. زيارة صفحة النشاط
   ↓
2. رؤية التصميم المخصص من صاحب النشاط
   ↓
3. التفاعل مع العناصر المفعلة
   ↓
4. الحجز أو الشراء
```

---

## 🎨 مكونات لوحة التحكم

### 1. منتقي الألوان (ColorCustomizer)
```typescript
interface ColorCustomizer {
  primary: string;      // اللون الأساسي
  secondary: string;    // اللون الثانوي
  accent: string;       // لون التركيز
  background: string;   // لون الخلفية
  text: string;        // لون النص
  
  // معاينة فورية
  preview: boolean;
}
```

### 2. تبديل العناصر (ElementToggler)
```typescript
interface ElementToggler {
  showHero: boolean;
  showGallery: boolean;
  showReviews: boolean;
  showMenu: boolean;
  showProducts: boolean;
  showBookings: boolean;
  showContact: boolean;
  showMap: boolean;
}
```

### 3. تخصيص الأزرار (ButtonCustomizer)
```typescript
interface ButtonCustomizer {
  primaryText: string;
  primaryColor: string;
  secondaryText: string;
  secondaryColor: string;
  showWhatsApp: boolean;
  showPhone: boolean;
  showEmail: boolean;
}
```

### 4. لوحة المعاينة (PreviewPanel)
- عرض مباشر للتغييرات
- تحديث فوري عند التغيير
- عرض الجوال والويب

---

## 🔗 الربط بين الصفحات

### المطاعم:
```
Dashboard: /merchant/dashboard/[id]
  ├── Color Customizer
  ├── Menu Customizer
  ├── Element Toggler
  └── Preview
       ↓
Display Page: /restaurants/[id]
  ├── Hero (مخصص)
  ├── Menu (مخصص)
  ├── Reviews
  ├── Booking
  └── Contact
```

### المحلات:
```
Dashboard: /merchant/dashboard/[id]
  ├── Color Customizer
  ├── Product Customizer
  ├── Element Toggler
  └── Preview
       ↓
Display Page: /stores/[id]
  ├── Hero (مخصص)
  ├── Products (مخصص)
  ├── Reviews
  ├── Booking
  └── Contact
```

### الحجوزات:
```
Dashboard: /merchant/dashboard/[id]
  ├── Booking Settings
  ├── Calendar Customizer
  ├── Element Toggler
  └── Preview
       ↓
Display Page: /bookings/[id]
  ├── Booking Form (مخصص)
  ├── Calendar
  ├── Confirmation
  └── Contact
```

---

## 📝 خطوات التنفيذ

### المرحلة 1: إعداد قاعدة البيانات
- [ ] تحديث Merchant Schema
- [ ] إضافة Customization fields
- [ ] إنشاء API endpoints

### المرحلة 2: بناء مكونات التخصيص
- [ ] ColorCustomizer
- [ ] ElementToggler
- [ ] ButtonCustomizer
- [ ] MediaUploader
- [ ] PreviewPanel

### المرحلة 3: بناء لوحة التحكم
- [ ] Merchant Dashboard
- [ ] Restaurant Dashboard
- [ ] Store Dashboard
- [ ] Booking Dashboard

### المرحلة 4: بناء صفحات العرض
- [ ] Restaurant Display Page
- [ ] Store Display Page
- [ ] Booking Display Page
- [ ] Merchant Profile Page

### المرحلة 5: الربط والاختبار
- [ ] ربط Dashboard بـ Display Pages
- [ ] اختبار التحديثات الفورية
- [ ] اختبار الأداء

---

## 🎯 الميزات الرئيسية

✅ تخصيص الألوان بالكامل
✅ إظهار/إخفاء العناصر
✅ تخصيص الأزرار والنصوص
✅ رفع الصور والوسائط
✅ معاينة مباشرة
✅ حفظ تلقائي
✅ استعادة الإعدادات الافتراضية
✅ قوالب جاهزة

---

## 🚀 الحالة: جاهز للتنفيذ
